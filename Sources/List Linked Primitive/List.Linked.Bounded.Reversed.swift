// List.Linked.Bounded.Reversed.swift
// Tag namespace for __ListLinked.Bounded's borrowing `reversed` accessor.

extension __ListLinked.Bounded where Element: ~Copyable, S: ~Copyable {
    /// Tag namespace for the borrowing `reversed` accessor.
    public enum Reversed {}
}
