// List.Linked.Bounded.Peek.swift
// Tag namespace for __ListLinked.Bounded's borrowing `peek` accessor.

extension __ListLinked.Bounded where Element: ~Copyable, S: ~Copyable {
    /// Tag namespace for the borrowing `peek` accessor.
    public enum Peek {}
}
